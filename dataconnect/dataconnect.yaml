specVersion: "v1beta"
serviceId: "appointment-scheduling-system"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "appointment-scheduling-system-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
